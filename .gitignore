node_modules/
# Local pi config. Do NOT track .pi/settings.json: if a contributor also has
# this package installed, loading it twice registers the isub*/artifact tools
# twice and pi refuses to start.
.pi/*
!.pi/skills/

# VS Code local history
.history/

# Copilot artifacts
copilot_all_tools.jsonl
